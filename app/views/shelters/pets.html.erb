<h1><%= @shelter.name %>: Pets</h1>

<% @pets.each do |pet| %>
  <img src="<%= pet.image %>" alt="<%= pet.name %>">
  <h3><%= pet.name %></h3>
  <p><%= pet.age %></p>
  <p><%= pet.sex %></p>
<% end %>